# Architecture

DataGovSecOps extends governed data products with machine-readable security intent and assurance evidence.

The model separates:

- control objective;
- accountable owner;
- implementation statement;
- verification method;
- evidence reference;
- exception and expiry;
- runtime enforcement readiness.

Declarative metadata supports automation but does not itself establish that a control is effective.
